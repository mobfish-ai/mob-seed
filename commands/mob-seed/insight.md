---
name: mob-seed:insight
description: 外部洞见管理 - 导入、评估和采纳外部知识（论文、博客、专家意见等）
allowed-tools: Read, Write, Edit, Bash, Task, TodoWrite, AskUserQuestion
argument-hint: [URL] [--text] [--list] [--stats] [--review] [--update=<id>]
---

# /mob-seed:insight - 外部洞见管理

> **核心理念**: 洞见是来自外部源（专家意见、论文、博客等）的知识单元，通过系统化的评估和采纳流程，融入项目的方法论体系。

---

## 执行步骤

### 步骤 0: 版本显示（遵循 SKILL.md 强制启动行为）

> 遵循 SKILL.md "🚀 强制启动行为" 章节定义，显示版本和场景信息。

---

## 命令概览

```
/mob-seed:insight [options]

用法:
  /mob-seed:insight "https://..."     # 从 URL 快速导入
  /mob-seed:insight --text            # 交互式文本导入
  /mob-seed:insight --list            # 列出洞见
  /mob-seed:insight --stats           # 统计信息
  /mob-seed:insight --review          # 模型时代复审
  /mob-seed:insight --update <id>     # 更新状态
```

---

## 存储模式

洞见支持两种存储模式：

| 模式 | 适用场景 | 配置方式 | 存储位置 |
|------|----------|----------|----------|
| 默认模式 | 非开源项目 | 无需配置 | `.seed/insights/` |
| 外部+软链接模式 | 开源项目 | 配置 `ace.output_dir` | 外部目录，软链接到 `.seed/` |

### 配置优先级

```
1. 环境变量 ACE_OUTPUT_DIR（最高）
2. .seed/config.local.json
3. .seed/config.json
4. 默认值 .seed/insights/（最低）
```

### 配置示例

```json
// .seed/config.json 或 .seed/config.local.json
{
  "ace": {
    "output_dir": "~/knowledge-base/mob-seed",
    "insight": {
      "template": "path/to/custom-template.md",
      "methodologies": ["ACE", "SEED", "LINKS", "FLEET"],
      "comparison_label": "方法论关联",
      "comparison_note": "与项目核心方法论中相关的做对比"
    }
  }
}
```

### 洞见配置项 (`ace.insight`)

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `template` | mob-seed 内置模板 | 自定义洞见模板路径 |
| `methodologies` | `["mob-seed"]` | 对比方法论列表 |
| `comparison_label` | `"方法论关联"` | 对比章节标题 |
| `comparison_note` | `"与 mob-seed 做对比分析"` | 对比说明文字 |

配置后，系统会在目标目录创建 `insights/` 子目录，并在 `.seed/` 下创建软链接。

---

## 索引架构 (v2.0)

为优化大规模洞见管理，采用分离式索引架构：

### 文件结构

```
insights/
├── index.json          # 精简索引（快速查询）
├── tags-index.json     # 标签倒排索引（按标签搜索）
├── index-full.json.bak # 完整索引备份（可删除）
└── ins-*.md            # 洞见文件（完整元数据在 frontmatter）
```

### index.json（精简版）

仅保留必要字段用于快速列表和状态查询：

```json
{
  "version": "1.0.0",
  "updated": "2026-01-22T12:00:00.000Z",
  "count": 115,
  "insights": [
    {"id": "ins-xxx", "status": "evaluating", "date": "2026-01-22", "file": "ins-xxx.md"}
  ]
}
```

### tags-index.json（标签倒排索引）

用于按标签快速查找洞见，仅索引出现 2 次以上的标签：

```json
{
  "version": "1.0.0",
  "updated": "2026-01-22T12:00:00.000Z",
  "stats": {"total_tags": 767, "indexed_tags": 148, "threshold": 2},
  "tags": [
    {"tag": "claude-code", "count": 46, "insights": ["ins-xxx", "ins-yyy"]},
    {"tag": "skills", "count": 34, "insights": ["ins-aaa", "ins-bbb"]}
  ]
}
```

### 完整元数据获取

当需要完整元数据（author, affiliation, source, tags 等）时，从洞见文件的 frontmatter 读取：

```bash
# 读取单个洞见完整信息
Read ins-xxx.md → 解析 YAML frontmatter
```

### 更新操作

| 操作 | 更新文件 |
|------|----------|
| 新增洞见 | index.json + tags-index.json + ins-*.md |
| 更新状态 | index.json + ins-*.md frontmatter |
| 按标签搜索 | 仅读取 tags-index.json |
| 列出洞见 | 仅读取 index.json |
| 查看详情 | 读取 ins-*.md |

---

## 洞见生命周期

```
evaluating (评估中)
    ↓
    ├──→ piloting (试点中) ──→ adopted (已采纳)
    │                     └──→ partial (部分采纳)
    │                     └──→ rejected (已拒绝)
    ├──→ adopted (直接采纳)
    ├──→ partial (部分采纳)
    └──→ rejected (已拒绝)

adopted/partial ──→ obsolete (已过时) ──→ evaluating (重新评估)
rejected ──→ evaluating (重新评估)
```

---

## 命令详解

### 1. 从 URL 导入

```
/mob-seed:insight "https://example.com/article"
```

**执行流程**:

1. **抓取内容**: 获取 URL 内容
2. **AI 辅助提取**: 自动提取元数据
   - 标题、作者、机构
   - 发布日期
   - 来源类型推断
   - 可信度评估
   - 标签建议
3. **生成洞见文件**: 创建 `ins-{YYYYMMDD}-{slug}.md`
4. **更新索引**:
   - 添加精简记录到 `index.json`（id, status, date, file）
   - 更新 `tags-index.json` 中相关标签的洞见列表

**输出示例**:

```
🔍 正在抓取: https://example.com/article

📄 提取结果:
   标题: 关于 AI 编程最佳实践的思考
   作者: John Doe (TechCorp)
   日期: 2026-01-10
   类型: blog
   可信度: medium
   标签: [ai, coding, best-practices]

✅ 洞见已创建: ins-20260111-ai-coding-best-practices
   位置: .seed/insights/ins-20260111-ai-coding-best-practices.md
   状态: evaluating (待评估)

💡 下一步:
   - 编辑文件完成评估笔记
   - 使用 /mob-seed:insight --update <id> 更新状态
```

### 2. 交互式文本导入

```
/mob-seed:insight --text
```

**执行流程**:

1. **提示输入**: Claude 引导用户粘贴内容
2. **提示来源信息**: 询问来源类型、作者等
3. **AI 辅助分析**: 提取关键观点和标签
4. **生成洞见文件**: 完整元数据写入 frontmatter
5. **更新索引**: 同步更新 `index.json` 和 `tags-index.json`

### 3. 列出洞见

```
/mob-seed:insight --list [--status <status>] [--tag <tag>]
```

**输出示例**:

```
📚 洞见列表 (共 12 条)

🔍 评估中 (3)
├── ins-20260111-ai-coding-best-practices
│   标题: 关于 AI 编程最佳实践的思考
│   来源: blog | 可信度: medium
│   标签: ai, coding
│
├── ins-20260110-llm-context-management
│   标题: LLM 上下文管理技巧
│   来源: paper | 可信度: high
│   标签: llm, context

✅ 已采纳 (5)
├── ins-20260105-tdd-for-ai
│   标题: AI 辅助 TDD 实践
│   来源: expert_opinion | 可信度: high
│   标签: tdd, ai
...

📊 统计: 评估中 3 | 试点中 1 | 已采纳 5 | 部分采纳 2 | 已拒绝 1
```

### 4. 统计信息

```
/mob-seed:insight --stats
```

**输出示例**:

```
📊 洞见统计

按状态:
├── 🔍 评估中:   3
├── 🧪 试点中:   1
├── ✅ 已采纳:   5
├── ⚡ 部分采纳: 2
├── ❌ 已拒绝:   1
└── 📦 已过时:   0

按来源类型:
├── expert_opinion: 4
├── paper:          3
├── blog:           3
├── documentation:  2

按可信度:
├── high:   5
├── medium: 5
├── low:    2
```

### 5. 模型时代复审

```
/mob-seed:insight --review
```

**功能**: 检查是否有洞见需要因模型升级而复审

**输出示例**:

```
🔄 模型时代复审检查

当前模型时代: claude-opus-4-6

⚠️ 需要复审的洞见 (3 条):

1. ins-20250601-prompt-patterns (claude-3-opus)
   标题: Prompt 设计模式
   状态: adopted
   原因: 模型时代落后 4 个版本
   建议: 验证 Prompt 模式在新模型上是否仍然有效

2. ins-20250815-context-limits (claude-3.5-sonnet)
   标题: 上下文限制处理策略
   状态: adopted
   原因: 模型时代落后 3 个版本
   建议: 检查上下文限制是否已改变

💡 使用 /mob-seed:insight --update <id> obsolete 标记过时
   或编辑文件重新评估
```

### 6. 更新状态

```
/mob-seed:insight --update <id> <new-status>
```

**有效状态转换**:

| 当前状态 | 可转换到 |
|----------|----------|
| evaluating | piloting, adopted, partial, rejected |
| piloting | adopted, partial, rejected |
| adopted | obsolete |
| partial | obsolete, adopted |
| rejected | evaluating |
| obsolete | evaluating |

**输出示例**:

```
✅ 状态更新成功

洞见: ins-20260111-ai-coding-best-practices
转换: evaluating → adopted
标签: 直接采纳

💡 已更新:
   - 洞见文件 frontmatter 状态字段
   - index.json 中的状态记录
```

---

## 洞见文件格式

> **完整模板**: 参见 `skills/mob-seed/templates/insight.md`
>
> 以下为 frontmatter 规范概览，完整文件结构以模板为准。

```yaml
---
id: ins-{YYYYMMDD}-{slug}
source:
  title: "文章/分享标题"
  type: blog | expert_opinion | experience | documentation | paper | discussion | video | interview | report | article | community | ai_generated
  author: "作者名"
  affiliation: "所属机构"
  date: YYYY-MM-DD
  context: "分享/发布背景"
  url: "原始链接"
  credibility: high | medium | low
  secondary_sources:
    - title: "支持来源1"
      url: "链接"
date: YYYY-MM-DD
status: evaluating | piloting | adopted | partial | rejected | obsolete
model_era: claude-opus-4-6
review_trigger: model_upgrade | time_elapsed | manual
tags: [tag1, tag2]
---
```

**必需章节**（详见模板）:
- `## 原始洞见` — 核心概念、解决的问题、架构设计/方法论、详细分析
- `## 评估笔记` — 适用性表格、方法论关联分析（可配置）、可借鉴设计、关键洞察、局限性
- `## 采纳决策` — 采纳/观望/不采纳三分类
- `## 相关变更` — 关联洞见（`[[wikilink]]`）、来源追溯、潜在行动
- `## 补充说明` — 项目/来源信息、关键引用、核心价值

---

## 来源类型说明

| 类型 | 说明 | 可信度参考 |
|------|------|------------|
| blog | 技术博客或文章 | 需评估作者背景 |
| expert_opinion | 领域专家的意见或建议 | 通常 high |
| experience | 个人或团队实践经验 | 需验证 |
| documentation | 官方文档或指南 | 通常 high |
| paper | 学术论文或研究报告 | 通常 high |
| discussion | 社区讨论（如 Twitter、论坛） | 需验证 |
| video | 视频内容（演讲、教程） | 需评估来源 |
| interview | 采访或对话记录 | 需评估权威性 |
| report | 行业报告或研究报告 | 通常 medium-high |
| article | 新闻报道或综合性文章 | 需验证 |
| community | 社区生态内容（开源项目、用户案例） | 需验证 |
| ai_generated | AI 生成的建议 | 需严格验证 |

---

## AI 辅助评估引导

当用户需要评估洞见时，Claude 应引导进行辩证分析：

```
📋 洞见评估引导

1. 核心观点提取
   - 这个洞见的主要论点是什么？
   - 有哪些支持论据？

2. 适用性分析
   - 这个观点在什么场景下适用？
   - 有哪些前提条件？
   - 与当前项目的契合度如何？

3. 局限性思考
   - 这个观点有什么局限性？
   - 可能存在什么反例？
   - 随着技术发展可能需要修正吗？

4. 采纳决策
   - 采纳全部、部分还是观望？
   - 如果采纳，具体在哪些方面应用？
   - 如何在项目中体现这个洞见？
```

---

## 与 ACE 的关系

洞见是 ACE (Agentic Context Engineering) 生态的重要组成部分：

```
ACE 目录结构:
.seed/
├── observations/    # 内部观察（自动生成）
├── reflections/     # 反思记录
├── insights/        # 外部洞见（本功能）
└── learning/        # 模式学习
```

洞见与观察的区别：

| 维度 | 观察 (Observation) | 洞见 (Insight) |
|------|-------------------|----------------|
| 来源 | 项目内部执行反馈 | 外部知识源 |
| 生成 | 自动触发 | 用户主动导入 |
| 格式 | obs-{date}-{slug} | ins-{date}-{slug} |
| 流程 | 归类 → 反思 → 升级 | 导入 → 评估 → 采纳 |

---

## 错误处理

| 错误场景 | 提示信息 | 解决方案 |
|----------|----------|----------|
| URL 无法访问 | ❌ 无法抓取 URL: {error} | 检查网络或 URL 有效性 |
| 内容提取失败 | ❌ 无法提取内容，请使用 --text 手动输入 | 使用文本导入模式 |
| 无效状态转换 | ❌ 无法从 {from} 转换到 {to}，允许: {allowed} | 参考状态转换规则 |
| 洞见不存在 | ❌ 找不到洞见: {id} | 使用 --list 确认 ID |
| 索引损坏 | ⚠️ 索引需要重建 | 自动重建索引 |
| 软链接目标不存在 | ❌ 外部目录不存在: {path} | 创建目标目录或检查配置 |

---

## 示例工作流

### 1. 导入专家意见

```bash
# 1. 从 URL 导入
/mob-seed:insight "https://twitter.com/expert/status/123456"

# 2. 编辑生成的文件，完成评估
# 3. 采纳
/mob-seed:insight --update ins-20260111-expert-advice adopted
```

### 2. 处理技术博客

```bash
# 1. 从文本导入
/mob-seed:insight --text
# 粘贴博客内容，按提示填写来源信息

# 2. 先试点
/mob-seed:insight --update ins-20260111-blog-insight piloting

# 3. 验证后全面采纳
/mob-seed:insight --update ins-20260111-blog-insight adopted
```

### 3. 模型升级后复审

```bash
# 1. 检查需复审的洞见
/mob-seed:insight --review

# 2. 对过时的洞见重新评估或标记过时
/mob-seed:insight --update ins-20250601-old-pattern obsolete

# 3. 或者重新评估
/mob-seed:insight --update ins-20250601-old-pattern evaluating
```
