---
description: SEED E阶段 - 自动派生（从规格生成代码/测试/文档）
allowed-tools: Read, Write, Edit, Bash, Task, TodoWrite, AskUserQuestion
argument-hint: <规格文件> [--code|--test|--docs|--all] [--dry-run]
---

# mob-seed-emit - 自动派生

执行内容：$ARGUMENTS

---

## E 阶段核心原则

> **Emit（自动派生）**: 所有产物必须从规格自动生成，禁止手动创建。

---

## 📦 依赖资源

```
.claude/skills/mob-seed/
├── prompts/
│   ├── emit-code.md        # 代码派生指导
│   ├── emit-test.md        # 测试派生指导
│   └── emit-docs.md        # 文档派生指导
└── adapters/
    ├── emit-engine.js      # 派生引擎
    └── seed-utils.js       # 工具模块
```

**项目配置**: `.seed/config.json`（由 `/mob-seed-init` 生成）

---

## 执行步骤

### 步骤 0: 检查初始化状态并加载配置

1. **检查 SEED 是否已初始化**：
   - 检查 `.seed/config.json` 是否存在
   - 如不存在，提示用户运行 `/mob-seed-init`

2. **加载配置获取路径**：
```javascript
const config = loadSeedConfig();
const SPECS_DIR = config.paths.specs;
const SRC_DIR = config.paths.src;
const TEST_DIR = config.paths.test;
const DOCS_DIR = config.paths.docs;
const OUTPUT_DIR = config.paths.output;
```

3. **动态检测技能目录**：
```bash
if [ -d ".claude/skills/mob-seed" ]; then
    SKILL_DIR=".claude/skills/mob-seed"
elif [ -d "$HOME/.claude/skills/mob-seed" ]; then
    SKILL_DIR="$HOME/.claude/skills/mob-seed"
fi
```

### 步骤 1: 解析参数

| 参数 | 模式 | 说明 |
|------|------|------|
| `--code` | 代码派生 | 只生成代码 |
| `--test` | 测试派生 | 只生成测试 |
| `--docs` | 文档派生 | 只生成文档 |
| `--all` | 全量派生 | 生成所有产物（默认） |
| `--dry-run` | 预览模式 | 只显示将要生成的内容，不实际写入 |

### 步骤 2: 读取规格文件

1. 定位规格文件：`{config.paths.specs}/{name}.fspec.md`
2. 解析规格内容：
   - 提取需求列表（FR-xxx）
   - 提取验收标准（AC-xxx）
   - 提取派生产物路径

### 步骤 3: 执行派生

> ⚠️ **重要**: 派生必须生成 **完整可运行的实现代码**，禁止生成空壳或 TODO 占位符。

#### 3.1 代码派生 (--code)

1. 读取派生指导：`$SKILL_DIR/prompts/emit-code.md`
2. 根据规格中的需求（FR/NFR）生成 **完整实现代码**：
   - 实现所有功能逻辑，不留 `TODO` 或 `throw new Error('Not implemented')`
   - 包含参数校验、错误处理、边界情况
   - 遵循项目代码风格和约定
3. 输出到配置路径：`{config.paths.src}/`

#### 3.2 测试派生 (--test)

1. 读取派生指导：`$SKILL_DIR/prompts/emit-test.md`
2. 根据验收标准（AC）生成 **完整可运行的测试**：
   - 每个 AC 生成具体测试用例（Given/When/Then）
   - 包含真实断言，不使用 `assert.ok(true, 'TODO')`
   - 覆盖正常路径和错误路径
3. 输出到配置路径：`{config.paths.test}/`

#### 3.3 文档派生 (--docs)

1. 读取派生指导：`$SKILL_DIR/prompts/emit-docs.md`
2. 根据规格生成完整使用文档
3. 输出到配置路径：`{config.paths.docs}/`

### 步骤 4: 记录派生关系

生成派生清单（manifest）：

```json
{
  "spec": "specs/user-login.fspec.md",
  "emittedAt": "2024-12-28T10:30:00+08:00",
  "outputs": [
    {
      "type": "code",
      "path": "src/auth/login.js",
      "hash": "abc123..."
    },
    {
      "type": "test",
      "path": "test/auth/login.test.js",
      "hash": "def456..."
    }
  ]
}
```

### 步骤 5: 输出结果

```
{config.paths.src}/
└── {module}/{file}.js      # 派生的代码

{config.paths.test}/
└── {module}/{file}.test.js # 派生的测试

{config.paths.docs}/
└── {module}/{file}.md      # 派生的文档

{config.paths.output}/
├── seed-manifest.json      # 派生清单
└── emit-report.md          # 派生报告
```

> 注：所有路径从 `.seed/config.json` 读取，适配任何项目结构

---

## 派生规则

### 规格 → 代码映射

| 规格内容 | 代码产物 |
|----------|----------|
| 功能需求 (FR) | 函数/方法实现 |
| 非功能需求 (NFR) | 性能优化、错误处理 |
| 约束 | 类型定义、校验逻辑 |
| 接口定义 | API 路由、参数处理 |

### 规格 → 测试映射

| 规格内容 | 测试产物 |
|----------|----------|
| 验收标准 (AC) | 测试用例 |
| Given | beforeEach/setup |
| When | 测试操作 |
| Then | assert/expect |

### 规格 → 文档映射

| 规格内容 | 文档产物 |
|----------|----------|
| 概述 | 简介章节 |
| 需求 | 功能说明 |
| 使用示例 | 代码示例 |
| 接口定义 | API 文档 |

---

## 使用示例

```bash
# 全量派生（默认）
/mob-seed-emit "specs/user-login.fspec.md"

# 只生成代码
/mob-seed-emit "specs/user-login.fspec.md" --code

# 只生成测试
/mob-seed-emit "specs/user-login.fspec.md" --test

# 预览模式
/mob-seed-emit "specs/user-login.fspec.md" --dry-run

# 指定多个派生类型
/mob-seed-emit "specs/user-login.fspec.md" --code --test
```

---

## 进度显示

| 图标 | 含义 |
|------|------|
| 📄 | 读取规格 |
| ⚙️ | 正在派生 |
| 📦 | 生成产物 |
| ✅ | 派生完成 |
| ⚠️ | 派生警告 |
| ❌ | 派生失败 |

示例输出：
```
🌱 SEED E阶段: 自动派生
━━━ 📄 读取规格 ━━━
规格: specs/user-login.fspec.md
需求: 3 FR, 2 NFR
验收: 4 AC

━━━ ⚙️ 代码派生 ━━━
📦 src/auth/login.js (新建)
📦 src/auth/validators.js (新建)

━━━ ⚙️ 测试派生 ━━━
📦 test/auth/login.test.js (新建)

━━━ ⚙️ 文档派生 ━━━
📦 docs/auth/login.md (新建)

✅ E阶段完成
派生产物: 4 文件
清单: output/mob-seed/seed-manifest.json
```

---

## 冲突处理

当派生产物已存在时：

| 配置 | 行为 |
|------|------|
| `overwrite: false` | 跳过，保留现有文件 |
| `overwrite: true` | 覆盖现有文件 |
| `--force` | 强制覆盖（忽略配置） |

```bash
# 强制覆盖
/mob-seed-emit "specs/user-login.fspec.md" --force
```

---

## 派生引擎架构

```
┌─────────────────────────────────────────────────────────────┐
│                     派生引擎架构                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  规格文件 (.fspec.md)                                        │
│      │                                                       │
│      ▼                                                       │
│  ┌─────────────────────┐                                    │
│  │   规格解析器         │  解析 Markdown 结构                │
│  │   SpecParser        │                                    │
│  └─────────┬───────────┘                                    │
│            │                                                 │
│            ▼                                                 │
│  ┌─────────────────────┐                                    │
│  │   派生引擎          │  调度各类型派生                     │
│  │   EmitEngine        │                                    │
│  └─────────┬───────────┘                                    │
│            │                                                 │
│      ┌─────┼─────┐                                          │
│      ▼     ▼     ▼                                          │
│  ┌──────┐ ┌──────┐ ┌──────┐                                │
│  │ Code │ │ Test │ │ Docs │                                │
│  │Emitter│ │Emitter│ │Emitter│                              │
│  └──────┘ └──────┘ └──────┘                                │
│      │     │     │                                          │
│      ▼     ▼     ▼                                          │
│  ┌──────────────────────┐                                   │
│  │   文件写入器          │  写入文件 + 更新清单              │
│  │   FileWriter         │                                   │
│  └──────────────────────┘                                   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```
