# ACE 命令嵌入层规格

> 版本: 1.0.0
> 状态: archived
> 归档日期: 2026-01-03
> 创建时间: 2026-01-03
> 最后更新: 2026-01-03
> 模板: feature
> 层级: L2 (强)

---

## 概述 (Overview)

### 功能描述
在 exec.md 和 defend.md 命令末尾嵌入 ACE 收集器调用，确保命令执行后自动收集观察。

### 目标用户
所有使用 `/mob-seed:exec` 和 `/mob-seed:defend` 的开发者

### 核心价值
- 测试失败时自动收集 test_failure 观察
- 规格漂移时自动收集 spec_drift 观察
- 无需用户主动调用 ACE

### 使用场景
- 场景1: 用户运行 `/mob-seed:exec`，测试失败，自动记录观察
- 场景2: 用户运行 `/mob-seed:defend`，检测到漂移，自动记录观察

---

## 需求 (Requirements)

### 功能需求 (Functional Requirements)

- [x] FR-001: exec.md 末尾添加 ACE 收集步骤
- [x] FR-002: defend.md 末尾添加 ACE 收集步骤
- [x] FR-003: 收集器调用 `collectFromExecute()` / `collectFromDefend()`
- [x] FR-004: 收集结果输出 `💡 ACE: 收集 N 条观察`

### 非功能需求 (Non-Functional Requirements)

- [x] NFR-001: 轻量级 - ACE 步骤不影响命令主流程
- [x] NFR-002: 透明 - 仅在有观察时输出提示

---

## 约束 (Constraints)

### 技术约束
- 文件: commands/exec.md, commands/defend.md
- 调用: lib/ace/observation-collector.js

### 业务约束
- ACE 步骤作为最后一步执行
- 即使 ACE 失败也不影响命令结果

---

## 验收标准 (Acceptance Criteria)

### AC-001: exec.md 包含 ACE 步骤
- **Given**: exec.md 存在
- **When**: 读取命令步骤
- **Then**:
  - 最后一步为 "ACE 观察收集"
  - 包含调用 `collectFromExecute(result)` 的说明
  - 包含输出格式说明

### AC-002: defend.md 包含 ACE 步骤
- **Given**: defend.md 存在
- **When**: 读取命令步骤
- **Then**:
  - 最后一步为 "ACE 观察收集"
  - 包含调用 `collectFromDefend(result)` 的说明
  - 包含输出格式说明

### AC-003: 测试失败时收集观察
- **Given**: 项目有失败的测试
- **When**: 运行 `/mob-seed:exec`
- **Then**:
  - 输出 `💡 ACE: 收集 N 条观察`
  - `.seed/observations/` 下创建新的观察文件

### AC-004: 规格漂移时收集观察
- **Given**: 代码与规格不一致
- **When**: 运行 `/mob-seed:defend`
- **Then**:
  - 输出 `💡 ACE: 收集 N 条观察`
  - `.seed/observations/` 下创建新的观察文件

---

## 派生产物 (Derived Outputs)

| 类型 | 路径 | 说明 |
|------|------|------|
| 命令 | commands/exec.md | 添加 ACE 收集步骤 |
| 命令 | commands/defend.md | 添加 ACE 收集步骤 |

---

## 依赖 (Dependencies)

### 前置规格
- ace-skill-integration.fspec.md (L1)

### 外部依赖
- lib/ace/observation-collector.js (已存在)

---

## 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| 1.0.0 | 2026-01-03 | 初始版本 | Claude + User |
