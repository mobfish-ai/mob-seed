---
description: SEED 差异 - 比较规格与代码的差异
allowed-tools: Read, Bash, Glob, Grep
argument-hint: <spec-path> [--code] [--test] [--docs] [--all]
---

# mob-seed-diff

执行内容：$ARGUMENTS

## 📦 依赖资源

- 技能目录: `.claude/skills/mob-seed/`
- 工具模块: `adapters/seed-utils.js`
- 漂移检测: `prompts/defend-drift.md`
- **项目配置**: `.seed/config.json`（由 `/mob-seed-init` 生成）

## 执行步骤

### 步骤0: 检查初始化状态并加载配置

1. **检查 SEED 是否已初始化**：
   - 检查 `.seed/config.json` 是否存在
   - 如不存在，提示用户运行 `/mob-seed-init`

2. **加载配置获取路径**：
```javascript
const config = loadSeedConfig();
const SPECS_DIR = config.paths.specs;
const SRC_DIR = config.paths.src;
const TEST_DIR = config.paths.test;
const DOCS_DIR = config.paths.docs;
const OUTPUT_DIR = config.paths.output;
```

3. **动态检测技能目录**：
```bash
if [ -d ".claude/skills/mob-seed" ]; then
    SKILL_DIR=".claude/skills/mob-seed"
elif [ -d "$HOME/.claude/skills/mob-seed" ]; then
    SKILL_DIR="$HOME/.claude/skills/mob-seed"
fi
```

### 步骤1: 解析参数

从 `$ARGUMENTS` 中解析：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `<spec-path>` | 规格文件路径 | 必需 |
| `--code` | 只比较代码 | - |
| `--test` | 只比较测试 | - |
| `--docs` | 只比较文档 | - |
| `--all` | 比较所有（默认） | 默认 |

### 步骤2: 加载规格和派生清单

1. 解析规格文件，提取 FR 和 AC 列表
2. 加载对应的 manifest.json
3. 定位派生产物文件

### 步骤3: 执行差异比较

#### 代码差异（--code）

```
📝 代码差异: user-auth

规格定义的 FR:
  FR-001: 用户登录 ✅ 已实现
  FR-002: 用户登出 ✅ 已实现
  FR-003: 密码重置 ❌ 未实现

代码中额外的功能:
  + changeEmail() - 未在规格中定义
  + updateProfile() - 未在规格中定义
```

#### 测试差异（--test）

```
🧪 测试差异: user-auth

规格定义的 AC:
  AC-001: 登录成功 ✅ 有测试
  AC-002: 登录失败 ✅ 有测试
  AC-003: 密码重置流程 ❌ 无测试

测试中额外的用例:
  + "should handle network error" - 非 AC 测试
```

#### 文档差异（--docs）

```
📄 文档差异: user-auth

| 项目 | 规格 | 文档 | 状态 |
|------|------|------|------|
| 版本 | 1.2.0 | 1.1.0 | ⚠️ 过时 |
| FR数量 | 3 | 2 | ⚠️ 缺失 |
| 更新日期 | 12-28 | 12-20 | ⚠️ 过时 |
```

### 步骤4: 生成差异摘要

```
📊 差异摘要: user-auth

| 类型 | 匹配 | 缺失 | 额外 |
|------|------|------|------|
| 代码 | 2 | 1 | 2 |
| 测试 | 2 | 1 | 1 |
| 文档 | - | - | - |

总体同步率: 66%

建议操作:
1. 实现 FR-003 (密码重置)
2. 补充 AC-003 测试
3. 更新文档版本
```

## 输出格式

### 差异标记

| 标记 | 含义 |
|------|------|
| ✅ | 匹配/已实现 |
| ❌ | 缺失/未实现 |
| ⚠️ | 警告/过时 |
| + | 额外（代码有但规格无） |
| - | 缺失（规格有但代码无） |

### 差异类型

| 类型 | 说明 |
|------|------|
| MISSING | 规格定义但未实现 |
| EXTRA | 实现但规格未定义 |
| OUTDATED | 版本/内容过时 |
| MISMATCH | 内容不匹配 |

## 示例用法

```bash
# 查看所有差异
/mob-seed-diff specs/user-auth.fspec.md

# 只看代码差异
/mob-seed-diff specs/user-auth.fspec.md --code

# 只看测试差异
/mob-seed-diff specs/user-auth.fspec.md --test

# 只看文档差异
/mob-seed-diff specs/user-auth.fspec.md --docs
```
